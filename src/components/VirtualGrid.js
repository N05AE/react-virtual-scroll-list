import React, { Component } from 'react';

export class VirtualGrid extends Component {
  init = a => {
    return a;
  };

  render() {
    return (
      <div>
        Virtual Grid Virtual Grid Virtual Grid Virtual Grid Virtual Grid Virtual Grid Virtual Grid
        Virtual Grid Virtual Grid Virtual Grid Virtual Grid Virtual Grid Virtual Grid
      </div>
    );
  }
}
