import React from 'react';
import { VirtualGrid } from '../src/components/VirtualGrid';

export default {
  title: 'Virtual Grid',
  component: VirtualGrid,
};

export const normal = () => {
  return <VirtualGrid />;
};
